server:
  port: ${PORT:8088}
spring:
  datasource:
    # 未设置环境变量时使用本地默认值，云端部署通过 DB_URL/DB_USERNAME/DB_PASSWORD 覆盖
    url: ${DB_URL:jdbc:mysql://localhost:3306/movie_info1707?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:20170330Hyn@}
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: false
  global-config:
    db-config:
      table-underline: false