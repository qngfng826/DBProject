# 执行部署准备（本地开发完全不受影响）

1. **`Movie_Rating_Backend/src/main/resources/application.yml`**：数据库连接改为带默认值的环境变量（`${DB_URL:本地原值}` 等），端口改 `${PORT:8088}`
2. **`movie-rating-frontend/src/api/request.js`**：`baseURL` 改为 `import.meta.env.VITE_API_BASE_URL || '/api'`
3. **新建 `Movie_Rating_Backend/Dockerfile`**：多阶段构建（Maven+JDK8 打包 → JRE8 运行），限制 JVM 内存适配 Render 免费 512MB
4. **导出本地数据库**：`mysqldump` 导出 `movie_info1707` 到 `deploy/init.sql`（只读本地库，作为云端初始化脚本兼备份）
5. **新建 `DEPLOY.md`**：TiDB Cloud / Render / Cloudflare Pages / UptimeRobot 的逐步操作指南
6. 完成后验证：本地后端能否照常启动连本地库（确认默认值生效）、前端 `npm run build` 是否正常

注册第三方平台账号和在线点部署由你按 DEPLOY.md 操作（约 30 分钟），遇到问题随时找我。